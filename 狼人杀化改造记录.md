# 狼人杀化改造记录

> 本文件记录 my_ai_town 从“连续生活模拟”向“狼人杀式猫鼠玩法”改造的全部工作。
> 更新日期：2026-08-17

---

## 一、改造总览

在不新增美术/动作资产的前提下，把现有小镇改造成狼人杀式博弈：

- 昼夜回合：夜里暗杀，天亮 08:00 统一公布死讯
- 白天投票：18:00 镇民大会，19:30 开票放逐
- 胜负判定：卧底全灭 = 镇民胜；无辜全灭或无辜 ≤ 卧底 = 卧底胜
- 身份技能：医生守诊、图书管理员查验、卧底嫁祸、警察制服额度
- 暗杀节奏：只在夜间可用，全队每晚最多一刀

---

## 二、狼人杀骨架（第一批）

### ① 昼夜回合化
- `confirm_resident_death` 检测：夜间（20:00–08:00）且死因含“暗杀” → 死亡不入公告、不唤醒全镇，进入 `pendingDeathAnnouncements` 队列。
- 每天 08:00 flush：批量公告“清晨，于第X天…死亡”，补记公共事件日志，并唤醒全镇。
- 目击者仍当场拿到“目睹暗杀”事件（信息不对称保留）。

### ② 每日镇民大会投票
- 18:00 开会，唤醒快照携带 `snapshot.exile_vote`（候选人名单）。
- LLM 通过决策附件 `exile_vote` 投票（`target_resident_name + line`），不是新动作类型，避开动作注册。
- 19:30 开票：最高票放逐并公布身份；平票/零票流会。
- 18:00 唤醒使用 `wake_while_current_action=true`，进行中的活动不阻断投票。

### ③ 胜负判定
- 每次死亡后 `check_victory`：
  - 卧底全灭 → 镇民胜
  - 无辜全灭，或无辜人数 ≤ 卧底人数 → 卧底胜
- 世界里没有卧底居民时，整套狼人杀特性不激活（普通/fixture 世界不会误发胜负、不会开大会）。
- 夜间终局：`gameOver/winner` 当夜即锁，公告压到次日 08:00 与死讯一起揭晓。
- 终局后 19:30 不再开票：`settle_vote_round` 检查 `gameOver` 并清空投票。

---

## 三、P0/P1 加固（2026-08-16 夜间补丁）

- 无卧底世界整体停用：`feature_active` 同时守卫死亡延迟、大会、投票接口。
- 信息泄露封堵：
  - 夜间待公布死亡不写公共事件日志（08:00 补记）
  - 警察案件档案 / `get_public_death_events` 只显示已公布死亡
  - `TownResidentStateProjection` 在天亮前保持死者“在世”外观
  - wake 附近感知掩蔽“已经死亡”
- 夜间胜负“天亮再宣布”：`winnerAnnounced` 状态随存档走。
- 旧档兼容：无 `winnerAnnounced` 键的终局旧档恢复时视为已公告。

---

## 四、日志可读性 + 每次运行独立存档

- `world/runtime/TownLog.gd`：
  - 日志类别（AGENT/CATMOUSE/WEREWOLF/ROLE_SKILL）切换时自动空一行
  - `section(title)` 打阶段横幅：天亮、大会开始、开票、胜负、夜间行动
- 启动器：
  - `run-game-log.ps1`：每次运行生成 `logs\runs\godot-<时间戳>-pid<PID>.log`
  - `run-test-log.ps1`：测试日志独立到 `logs\tests\`
  - `tail-log.ps1`：默认追踪 `logs\runs` 最新一份
- 阶段日志时间使用 tick 分钟（`_label_for_minute`），避免跨分钟推进时横幅错标到终态时间。

---

## 五、身份技能第一批（2026-08-17）

### 设计原则
- 技能走决策附件 `night_skill`，不新增动作类型。
- 状态全部挂在 `werewolfState.roleSkills`，复用存档域；恢复时 `ensure_state` 自愈旧档。
- 20:00 唤醒技能持有者进入夜间行动阶段；08:00 先结算技能，再 flush 死讯。

### 技能清单
| 角色 | 技能 | 规则 |
|---|---|---|
| 白芷（医生） | `doctor_protect` 守诊 | 每夜守护 1 人；被守者当夜遇暗杀则被救下，不入死亡队列；不能连续两夜守同一人 |
| 许照（图书管理员） | `scholar_inspect` 查验 | 共 3 次；每夜查验 1 人行踪；08:00 收到行为线索（位置/同行者/是否移动），不给阵营答案 |
| 卧底阵营 | `undercover_frame` 嫁祸 | 全阵营共用 1 次；下一次暗杀案件的警察档案线索指向被嫁祸者 |
| 闻叙（警察） | 制服额度制 | 全场 2 次额度；错杀平民立即停职 + 全镇公告；暗杀死因对警察脱敏为“被暗杀” |

### 合同注册链（night_skill）
- `AgentContract.NIGHT_SKILL_FIELDS` 白名单
- `AgentContract.validate_decision` 决策字段白名单 + 校验入口
- `AgentContractSnapshot._validate_night_skill` + snapshot 结构校验
- `TownActionValidation.validate_decision_shape`
- `AgentContract.canonicalize_decision` 剥离多余字段
- `AgentPromptCompiler` 约束渲染
- `TownWorldRuntime` 快照注入 + 决策提交解析 → `TownRoleSkillRuntime.submit_night_skill`
- `EVENT_TYPES` 补充：目睹暗杀 / 守诊结果 / 查验结果 / 被救 / 暗杀失败

---

## 六、暗杀节奏（2026-08-17）

- 暗杀只能在夜间（20:00–08:00）：
  - 白天不注入 assassinate 选项
  - `_prepare_assassination_action` 白天拒绝
  - `_activate_assassination_action` 双保险拒绝
- 全队每晚最多一刀：
  - `werewolfState.undercoverKillLastNight` 按夜序去重
  - 谁先得手，当晚全队不能再杀
  - 被医生挡下同样消耗当晚机会
- 下一晚 20:00 自动恢复。

---

## 七、提示词更新

- `background/10_town_common_knowledge.md`：
  - 镇民大会规则、夜间身份技能公开规则
  - “阵营与胜负”：镇民阵营/卧底阵营及各自胜利条件
  - 明确“狼人杀式阵营博弈：白天投票、夜里卧底杀人、天亮公布死讯”
- `roles/undercover.md`：
  - 只有夜里能杀、全队每晚一次
  - **同伙名单**：谢眠（乐师）、乔一鸣（送货员）、花子（咖啡店店员），除自己外另外两人为同伙
  - 不能暗杀/嫁祸/投票放逐同伙
  - 嫁祸技能用法
  - 阵营胜利条件
- `roles/police.md`：
  - 制服额度 2 次、错杀停职
  - 暗杀死因脱敏、线索可能是嫁祸
  - 胜利条件
- `roles/resident_bai_zhi_01.md`：医生守诊规则 + 身份与目标（镇民阵营）
- `roles/resident_xu_zhao_01.md`：档案查验规则 + 身份与目标（镇民阵营）

提示词加载机制（`AgentPromptCompiler._load_role_prompt`）：
1. `me.is_undercover` → `undercover.md`
2. 职业为警察 → `police.md`
3. 存在 `prompts/roles/<resident_id>.md` → 追加个人规则

---

## 八、测试清单（headless，全部 PASS）

| 测试 | 结果 |
|---|---|
| `diag_role_skills.gd`（新增） | 94 checks |
| `diag_werewolf_vote.gd` | 71 checks |
| `town_world_foundation_test.gd` | 1185 checks |
| `town_world_agent_test.gd` | 967 checks |
| `resident_presentation_test.gd` | 1886 checks |
| `town_world_save_test.gd` | 179 checks |
| `town_world_action_type_registry_test.gd` | 79 checks |
| `agent_initialization_contract_test.gd` | PASS |
| `diag_role_rules.gd` | PASS（含白芷/许照/花子技能与同伙名单） |
| `diag_assassinate` / `range` / `indoor` / `witness` / `police_investigate` | 全部 PASS（已改为夜间场景） |

---

## 九、最新一次真实运行日志分析

日志文件：`logs/runs/godot-debug-20260817-021055.log`（1803 行，第 1 天 08:00 → 18:09，尚未进入 20:00 夜间阶段）

### 总体状态
- 引擎 ERROR / SCRIPT ERROR：0
- `[CATMOUSE]`：0（白天没有暗杀发生）
- `[WEREWOLF]` / `[ROLE_SKILL]`：0（尚未到 20:00，夜间技能未触发）
- **白天全程没有出现 assassinate/暗杀选项** → “暗杀只在夜间”的门禁在真实运行中已生效

### 请求失败统计
- 请求失败共 618 行，主要是两类：

#### 1. `模型调用失败`（421 行）——模型服务侧问题，不是代码 bug
- `error_type=rate_limit`：模型限流
- `error_type=empty_content`：模型返回空内容
- 随后的 `AGENT_CONTINUITY_FALLBACK_APPLIED` / `DEFERRED` 是**连续性兜底在工作**（让居民继续行动），不是错误。
- 处理方向：检查当前模型提供商的限流/配额/配置，不是改世界逻辑。

#### 2. `世界动作结果缺少匹配的居民原意图：dining-close-home:...`（11 行）
- 根因：食堂关餐时系统自己生成的回家动作 `dining-close-home:<id>:<minute>`（`TownDiningServiceRuntime._send_home`）会产生动作结果，但这个动作**不是 LLM 的原始意图**；证据队列 `ResidentEvidenceQueue.append_wake` 要求每条新动作结果都能匹配 `matched_intents`，匹配不到就拒绝整个唤醒包（`ResidentEvidenceQueue.gd:91-92`），于是出现 `AGENT_DECISION_REQUEST_REJECTED`。
- 影响：涉及食堂关餐的居民决策被拒一次，然后由 fallback 兜住；不会崩，但会刷日志并多一次回退。
- 修复方向（未实施）：
  - 证据队列对**系统生成的动作结果**放行/跳过（不带原意图也能入证据），或
  - `_send_home` 生成结果时打上“系统意图”标记，证据队列据此免匹配，或
  - 把系统动作结果从该居民的 `action_results` 里剔除，改为只投递事实事件。
  - 现有 `town_world_agent_test.gd` 已断言“continuity 不应因缺少匹配原意图进入循环”，说明这是需要按正式 bug 修复的场景。

#### 3. 零星契约拒绝（1 行）
- `action.line 提到了本轮动作不能使用的场景物件：窗口` → LLM 提交不合法动作，属于正常的合同拒绝与重试。

### 本次真实运行还未覆盖到
- 20:00 夜间行动、医生守诊、图书管理员查验、卧底嫁祸、每晚一刀、天亮公布死讯——因为日志只到 18:09。需要把游戏推进到 20:00 之后再做一轮真实验证。

---

## 十、遗留问题 / 下一步

1. **模型调用失败风暴**：先排查模型 provider（rate_limit / empty_content）与并发配置。
2. **dining-close-home 缺少原意图**：按上面的修复方向改证据队列，并补回归测试。
3. **死亡竞态**：居民死亡时已派发的 in-flight 决策仍可能完成后端打印（此前日志中“死者还在做决定”），需要取消/过滤死后请求。
4. **真实夜间阶段验证**：跑到 20:00 后检查 `[ROLE_SKILL]` 提交与 08:00 结算。
5. **警察制服/暗杀是否迁到附件**：暂不建议——新技能已走 `night_skill` 附件，旧动作保留成熟链路。
