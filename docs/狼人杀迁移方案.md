# 狼人杀改造迁移方案（本地 → 官方最新）

> 目标目录：`F:\my_ai_town_upstream`（worktree，分支 `wolf-migrate`，基于官方最新 `6675f77`）
> 源：`F:\my_ai_town`（本地 main + 工作区未提交修改）
> 日期：2026-08-24

## 一、背景结论

- 本地 fork 点：`f81be45`（官方 #58 合并后）
- 本地狼人杀改造规模：vs fork 点 **112 文件 / +11706 行**（另 17 个文件工作区未提交）
- 官方自 fork 点起推进 113 提交，其中 **#109「拆分小镇世界运行时架构」** 把 `TownWorldRuntime.gd` 从 23,145 行拆到 2,045 行（60+ 模块文件）
- 因此**不能 cherry-pick / 文件级合并**，必须**按官方新架构重新挂载**狼人杀逻辑

## 二、冲突面分类（vs 官方改动交集）

### A. 官方也改过 → 需三向适配（25 文件）
`AgentContract.gd`、`AgentPromptCompiler.gd`、`ResidentMemorySystem.gd`、`OpenAICompatibleModelProvider.gd`、`GenericOpenAICompatibleModelProvider.gd`、`TownWorldAgentGateway.gd`、`GameFlowHost.gd`、`TownRuntime.gd`、`TownUiAdapter.gd`、`TownUiRuntimeHost.gd`、`UnifiedConversationScreen.gd`、`TownWorldRuntime.gd`、`TownResidentArrivalRuntime.gd`、`TownActionProjection.gd`、`TownActionTypeRegistry.gd`、`TownActionValidation.gd`、`TownActivityScalars.gd`、`TownWorldRestorePeople.gd`、`TownWorldRestoreState.gd`、`TownWorldSaveCodec.gd`、`TownResidentStateProjection.gd`、`TownWorkSettlement.gd`、`town_world_foundation_test.gd`、`town_world_save_test.gd`、`.gitignore`

### B. 官方没动过 → 直拷即重放本地修改（安全）
`AgentConflictContract.gd`、`AgentConflictIntent.gd`、`AgentContractIdentity.gd`、`AgentContractSnapshot.gd`、`ResidentStateCodec.gd`、`TownConflictRuntime.gd`、`TownWorldEnvironment.gd`、`TownResidentLifecycleRuntime.gd`、`TownRelationshipEvidenceProgress.gd`、`TownWorldOpeningConfig.gd`、`TownNewGameOpeningCompiler.gd`、`TownWorldCharacterMovementQuery.gd`、`resident_catalog.json`、`fixtures/town_world_opening.json`、`town_world_action_type_registry_test.gd`、`resident_memory_system_test.gd`、`prompts/background/10_town_common_knowledge.md`、`prompts/rules/*.md`

### C. 纯新增文件（官方没有，直拷 + 挂载）
- 提示词：`prompts/roles/undercover.md`、`police.md`、`resident_bai_zhi_01.md`（医生）、`resident_xu_zhao_01.md`（查验）、`resident_lin_lan_01.md`、`resident_luo_yuan_01.md`、`resident_tang_xiaoman_01.md`
- 运行时：`TownWerewolfRuntime.gd`、`TownRoleSkillRuntime.gd`、`TownUndercoverDeadlineRuntime.gd(.uid)`、`TownLog.gd`
- 测试：`diag_*.gd`（约 30 个）+ `fixtures/town_residents_from_save.json`

### D. 本地杂项（不做，或按需后补）
自定义 API 增强、精力系统、天气强制、旅行者→化身改名、日志脚本、docs 笔记等

## 三、关键适配决策

1. **动作注册**：官方 `AgentContract.gd` 的 `ACTION_TYPES` / `EVENT_TYPES` 数组为基础，追加本地 `assassinate` / `subdue`（公告动作官方已有，**不再自研**）
2. **附件字段**：`exile_vote`（镇民大会投票）、`night_skill`（身份技能）→ `AgentContractSnapshot.gd` + `TownActionValidation.gd` 白名单（官方这两文件 B/A 类，Snapshot 可直拷、Validation 需适配）
3. **TWR 钩子落点**（本地 24,782 行 TWR → 官方新模块）：
   - 暗杀/制服**准备与选项注入** → `action/TownActionPreparationRuntime.gd` + `TownActionPreviewRuntime.gd`（或 `TownActionOptionDirectory.gd`）
   - 暗杀/制服**激活结算** → `action/TownConfirmedActionActivationRuntime.gd` + `TownActionResultRuntime.gd`
   - 死亡结算 `confirm_resident_death` → `lifecycle/TownResidentLifecycleRuntime.gd`
   - 目击者事件 → `event/` 模块（官方有独立 event 目录）
   - 死亡公告三态 → **官方公告系统**（`announcement_published` 信号 / `TownAnnouncementPublisherProjection.gd`）
   - 镇民大会 18:00/19:30 定时 → `time/` 推进钩子 + `TownWerewolfRuntime` 自持
   - `werewolfState` 存档域 → `persistence/TownWorldSaveCodec.gd` + `TownWorldRestoreState.gd`（官方也改了 → 需适配）
   - 禁补位 `_poll_resident_replacement` → `GameFlowHost.gd`（官方也改了 → 需适配）
   - 夜间死者外观 → `presentation/TownResidentStateProjection.gd`（需适配）
4. **TownWerewolfRuntime / TownRoleSkillRuntime / TownUndercoverDeadlineRuntime / TownLog** 本身是独立模块（本地已模块化），直拷后仅需改挂载点

## 四、执行顺序

1. **阶段 1（直拷层）**：C 类全部 + B 类覆盖（官方未改文件直接拷本地版 = 三向合并结果）
2. **阶段 2（契约适配）**：A 类中契约/提示词编译/动作注册相关 → 以官方版为基底重放本地增量
3. **阶段 3（世界运行时挂载）**：TWR 狼人杀钩子重新挂到官方新模块 + 存档域 + Gateway + GameFlowHost + UI
4. **阶段 4（验证）**：Godot headless 导入无错 → diag_werewolf_vote / diag_role_skills / diag_assassinate 系列全绿 → 回归官方测试

## 五、验证手段

- `F:\tools\godot\Godot_v4.7.1-stable_win64_console.exe --headless --path <dir> --script res://tests/<diag>.gd`
- 官方自带测试套件：`game/tests/run_formal_release_story_suite.sh`（需 bash）

## 六、实施结果与验证修复记录（2026-08-22）

### 阶段 1-3 完成，全部 diag 与官方回归测试通过

**diag 测试（狼人杀特性）10/10 全绿**：
- 契约层：diag_intent_kill / diag_kill_alias / diag_role_rules / diag_conversation_prompt / diag_wealth_evidence / diag_wealth_render
- 运行时：diag_role_skills(94) / diag_werewolf_vote(71) / diag_assassinate 系列(14+8+12+15) / diag_announcement_action(11) / diag_police_investigate(19) / diag_undercover / diag_xie_baizhi(2) / diag_undercover_attack_option(3)

**官方回归测试全部通过**：town_world_foundation_test(1545) / town_world_save_test(181) / town_world_agent_test(1004) / town_world_agent_gateway_continuity_test / town_activity_test(1669) / resident_presentation_test(2877) / session_save_continue_roundtrip_test(28) / town_resident_content_test(392) / town_space_view_sync_test(42) / town_session_production_composition_test / town_world_action_type_registry_test(79)

### 修复的真实 bug（迁移遗漏）
1. **TownWorldRuntime 缺 _undercover_declare_police_failed**（TownUndercoverDeadlineRuntime 无辜全灭分支会 SCRIPT ERROR）→ 已补：幂等 flag + broadcast_announcement + _append_public_event_log
2. **check_deadline 未接线**（第6/7/8天处决机制从未运行）→ TownWorldAdvanceRuntime 分钟循环尾挂 UNDERCOVER_DEADLINE_RUNTIME.check_deadline

### 4 个卡死 diag 修复（本地遗留缺陷，本地仓库同样卡）
- 卡死机制：测试调旧 TWR 内部 API（_apply_sleep_activity_availability / _decorate_conflict_tension_options / _build_resident_perception 等）→ 官方拆分架构无此方法 → SCRIPT ERROR 中断 _initialize → quit() 不执行 → 空转；或 advance 参数单位错误（5*1440*60 秒=300天）超时
- diag_bed_ownership：重写为测官方 TownActivityScalars.apply_sleep_activity_availability（困→可用 / 不困→SLEEP_NOT_NEEDED）；**注意**：旧版"只能睡自己的床"校验不在新架构可用性层实现（睡觉选项按家生成）
- diag_undercover_indoor：重写为测 _prepare_assassination_action（夜间+同屋放行）
- diag_undercover_runtime：注入 3 卧底（_inject_undercover 范式同 diag_role_skills）+ advance 改精确到第6天23:59 + get_environment→world.get("_environment")；场景 B 改公告幂等断言
- diag_request_persist：上游 provider 无 _persist_request_record（本地请求落盘已被官方记忆写盘队列取代）→ has_method 守卫 SKIP

### 回退到官方契约的迁移改动
1. **REACTION_TEXT_MAX_LENGTH 64→32**（AgentContract.gd + TownActionValidation.gd；官方测试断言"最多 32 个字符"）
2. **AgentContractSnapshot 公告 reaction 绑定**：恢复官方"不属于本轮公告"报错（迁移时误改成忽略）
3. **MAX_CONCURRENT_MODEL_REQUESTS 4→6**（Gateway；官方并发通道测试断言 6；town_world_agent_test / town_world_agent_gateway_continuity_test 一并恢复官方版）
4. **meal_period_for_minute 回退官方边界**（早餐300-600/午餐600-840/晚餐960-1200；官方查询合同测试；"全天供餐"由官方新版食堂联动机制覆盖）

### 数据一致性修复（本地狼人杀角色 vs 官方数据契约）
- **resident_catalog.json 保留本地狼人杀版**（16 居民：闻叙=警察/白芷=草药医师/许照=图书管理员/谢眠=乐师等；B 类直拷实际未生效，属有意保留）
- **occupation_catalog.json 新增 occupation_police 职业模板**（镇公所 civic 链）+ defaultSocialStateCoverage 组合 default_social_police_town_hall 替换 default_social_town_manager_town_hall（16 组合对 16 居民）
- **work_chain_catalog.json 新增 occupation_police 工作链**（克隆镇管理者 civic 链）
- **TownWorldRestorePeople.gd socialState 白名单 +money/reputation**（本地注入字段）
- **town_world.json 重新构建**（Godot --headless --path game --script res://world/data/town/TownWorldDataBuild.gd，输出 TOWN_WORLD_DATA_BUILD_PASS）
- **TownSaveSchemaRegistry ACTIVITY_SOURCE_FINGERPRINT_AFTER_PUBLIC_DINING_DAY_REWORK 更新**为重建后指纹 bc3442e1...
- town_activity_test 适配：occupation_templates 15→16、supported_names 小镇管理者→警察

### 遗留说明
- 闻叙=警察在官方活动系统无专属职业活动链（_resolve_occupation 优雅回退空模板），其警察技能走 TownRoleSkillRuntime（job==警察）；如需正式工作活动可后续补 activity_definitions
- resident_memory_system_test 在官方/本地均不存在（方案文档 B 类列表笔误，实际文件为 resident_detail_memory_selection_test.gd）
- 本地 fork 另有未迁移项：OpenAICompatibleModelProvider 硬编码凭据（D 类不迁）
